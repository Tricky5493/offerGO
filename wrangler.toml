name = "offergo-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# Cloudflare Workers 免费计划限制
# KV 命名空间（可选，用于生产环境存储）
# kv_namespaces = [{ binding = "RESUME_CACHE", id = "your-kv-id" }]

# 环境变量（在 Cloudflare Dashboard 中设置更安全）
[vars]
ENVIRONMENT = "production"
